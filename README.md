# Raphael & Helia Wedding Invitation

Static wedding invitation prepared for GitHub Pages.

## Publish

Upload all files and folders to the repository root. In repository **Settings → Pages**, select **Deploy from a branch**, choose `main` and `/ (root)`, then save.
